import { html, when } from "@microsoft/fast-element";
export const AnchoredRegionTemplate = html `
    <template>
        ${when(x => x.initialLayoutComplete, html `
                <slot></slot>
            `)}
    </template>
`;
